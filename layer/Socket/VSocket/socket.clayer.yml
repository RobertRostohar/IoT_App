# yaml-language-server: $schema=https://raw.githubusercontent.com/Open-CMSIS-Pack/devtools/schemas/projmgr/0.9.1/tools/projmgr/schemas/clayer.schema.json

layer:
  # type: Socket
  # name: Virtual Socket
  description: IoT Socket interface connecting to Arm Virtual Hardware over Virtual Socket

  # interfaces:
  #   - consumes:
  #     - VHT
  #     - RTOS2:
  #   - provides:
  #     - IoT_Socket:

  # packs:
  #   - pack: MDK-Packs::IoT_Socket

  components:
    - component: MDK-Packs::IoT Utility:Socket:Custom

  groups:
    - group: Socket
      files: 
        - file: ./Socket/VSocket/socket_startup.c
        - file: ./Socket/VSocket/iot_socket.c
